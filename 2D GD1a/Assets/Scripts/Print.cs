using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Print : MonoBehaviour
{

    public string words;

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(words);  
    }

    // Update is called once per frame
    void Update()
    {
        // Debug.Log("A lot of text");
    }
}
